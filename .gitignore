# Archivos de desarrollo y documentación (no forman parte del plugin)
*.xlsx
*.pdf
desktop.ini
.DS_Store
Thumbs.db

# Dependencias (el usuario las descarga por separado)
# Descomenta la siguiente línea si prefieres no commitear la librería de updates:
# lib/
